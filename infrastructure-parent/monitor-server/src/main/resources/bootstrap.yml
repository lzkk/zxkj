spring:
  application:
    name: monitor-server
  profiles:
    active: dev
  security:
    user:
      name: "admin"
      password: "admin123"
      roles: ACTUATOR_ADMIN #拥有角色，用于允许自身访问

server:
  port: 12345

# Logger config
logging:
  config: classpath:log4j2.xml